<?xml version="1.0" encoding="utf-8"?>

<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="0dp"
    android:layout_height="match_parent"
    android:layout_weight="1"
    android:gravity="bottom|center_horizontal"
    android:orientation="vertical"
    android:paddingStart="3dp"
    android:paddingEnd="3dp">

    <TextView
        android:id="@+id/tvActivityCount"
        android:layout_width="wrap_content"
        android:layout_height="18dp"
        android:includeFontPadding="false"
        android:textColor="@color/tri_force_text_secondary"
        android:textSize="9sp"
        android:textStyle="bold" />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="82dp"
        android:layout_marginTop="3dp">

        <com.google.android.material.card.MaterialCardView
            android:id="@+id/barActivity"
            android:layout_width="match_parent"
            android:layout_height="4dp"
            android:layout_gravity="bottom"
            app:cardBackgroundColor="@color/tri_force_blue"
            app:cardCornerRadius="4dp"
            app:cardElevation="0dp"
            app:strokeWidth="0dp" />
    </FrameLayout>

    <TextView
        android:id="@+id/tvActivityDay"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_4"
        android:includeFontPadding="false"
        android:textColor="@color/tri_force_text_tertiary"
        android:textSize="9sp"
        android:textStyle="bold" />

</LinearLayout>
